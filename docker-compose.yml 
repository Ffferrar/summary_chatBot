# docker-compose для разработки
# Здесь будет описано:
# - сервис postgres (с pgvector)
# - сервис приложения (бот)
# В будущем можно добавить сервис для RAG, если он будет в том же compose.

version: "3.9"

services:
  db:
    image: postgres:16
    # TODO: добавить переменные окружения, volume, healthcheck и т.п.
    #       + включить расширение pgvector через init-скрипт

  app:
    build: .
    # TODO: настроить зависимости от db, env-файлы и команду запуска бота
