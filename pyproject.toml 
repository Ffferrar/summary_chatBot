# Базовый pyproject для инициализации проекта.
# Позже можно обновить через poetry init / add.

[tool.poetry]
name = "tg-rag-bot"
version = "0.1.0"
description = "Telegram RAG bot with Telethon and Postgres"
authors = ["Your Name <you@example.com>"]
package-mode = true

[tool.poetry.dependencies]
python = "^3.12"

[tool.poetry.group.dev.dependencies]
pytest = "^8.0.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
